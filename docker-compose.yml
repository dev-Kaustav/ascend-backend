services:
  app:
    build: .
    env_file: .env

    ports:
      - "127.0.0.1:8000:8000"

    environment:
      DATABASE_URL: ${DATABASE_URL}
      SECRET_KEY: ${SECRET_KEY}

    restart: unless-stopped